1. Create cluster: k3d cluster create --port '8082:30080@agent[0]' -p 8081:80@loadbalancer --agents 2
  - set PersistentVolume local path: docker exec k3d-k3s-default-agent-0 mkdir -p /tmp/kube
2. Install SealedSecrets: kubectl apply -f https://github.com/bitnami-labs/sealed-secrets/releases/download/v0.12.1/controller.yaml
  - Apply newly created sealedsecrets

4.8 --> Nats gets supported through kube-cluster-dwk
- When pushing with latest tags and having imagePullPolicy: Always --> Push changes to github, restarting pod after a while should result in an updated version. INCONCLUSIVE whether this is automatic or not. Very difficult exercise.

3. To install Nats manually
  - helm repo add nats https://nats-io.github.io/k8s/helm/charts/
  - helm repo update
  - helm install my-nats nats/nats